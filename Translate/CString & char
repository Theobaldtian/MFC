CString 转 char

UNICODE字符集

方法一：  该方法会将str拆分为一个个字符+\0的形式，而不是字符串
CString str = _T("Ab1");
char buf[20];
memcpy(buf, str, 20);

方法二：


================================================================================================

char 转 CString

UNICODE字符集

方法一：
